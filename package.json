{
  "name": "sticky-notes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "20.10.x || 22.12.x || >=23.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vitest": "^4.1.9"
  }
}
